<div class="block-container">
	<h3 class="search-title">Recipe Tags</h3>
	<div id="tag_cloud ">
	  <% tag_cloud Recipe.tag_counts, %w{s m l} do |tag, css_class| %>
	    <%= link_to tag.name, tag_path(tag.name), class: css_class %>
	  <% end %>
	</div>
</div>
